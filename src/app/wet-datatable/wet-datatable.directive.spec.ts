import { WetDatatableDirective } from './wet-datatable.directive';

describe('WetDatatableDirective', () => {
  it('should create an instance', () => {
    const directive = new WetDatatableDirective();
    expect(directive).toBeTruthy();
  });
});
